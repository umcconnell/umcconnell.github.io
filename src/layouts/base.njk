<!DOCTYPE html>
<html lang="{{ meta.lang }}">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>
        {{ meta.title }}{% if title %} | {{ title }}{% endif %}
    </title>

    <link rel="stylesheet" href="{{ '/assets/styles/main.css' | url }}">
    {% block css %}{% endblock %}
    {% include "meta.njk" %}
</head>
<body>
    <div hidden>{% iconsprite %}</div>
    <a href="#main" class="sr-skip-link">skip to main content</a>

    <div class="layout" role="document">
        {% include "header.njk" %}

        <main id="main" class="main" tabindex="-1">
        {% block content %}
            {{ content | safe }}
        {% endblock %}
        </main>

        {% include "footer.njk" %}
    </div>

    <script type="text/javascript" src="{{ '/assets/scripts/main.js' | url }}"></script>
    {% if (build.env === "production") and (meta.analytics) %}
    <script data-goatcounter="{{ meta.analytics }}" async src="//gc.zgo.at/count.js"></script>
    {% endif %}
</body>
</html>
